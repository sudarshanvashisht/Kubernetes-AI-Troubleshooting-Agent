apiVersion: v1
kind: Service
metadata:
  name: test-service-mismatch
  namespace: default
spec:
  selector:
    app: non-matching-app-label-xyz
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
